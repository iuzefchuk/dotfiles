return {
  {
    "akinsho/bufferline.nvim",
    opts = {
      options = {
        -- Keep the bufferline visible with a single buffer open too, so the tab
        -- strip doesn't appear and disappear as buffers are opened and closed.
        always_show_bufferline = true,
      },
    },
  },
}
